# How every committed artefact is regenerated, and how that claim is checked.
#
# This file exists because two regeneration recipes drifted from the artefacts
# they claim to produce, and nothing noticed. A recipe written only as prose is
# a claim no code checks. Here it is data, tests/test_recipes.py executes it,
# and the table in README.md is generated from it rather than maintained beside
# it.
#
# Fields
#   artefact  committed file the command produces, repository-relative
#   command   exactly what to run, from the repository root
#   label     short name, used in the README table
#   cost      human-readable, shown in the README table
#   seconds   measured wall-clock, for tiering
#   inputs    committed  every input is in the repository; runs on a fresh clone
#             local      needs data/raw or data/interim, which are gitignored
#             network    needs a large fetch; never run by the test suite
#   compare   bytes      output must be byte-identical to the committed file
#             subset     output reproduces a documented subset of the rows
#   out_flag  the flag that redirects this artefact, where the script writes
#             more than one file and a bare --out would be ambiguous
#   verified  continuously  a test runs the command and diffs, every suite run
#             on_local      the same, but only where the local inputs exist
#             on_demand     too expensive to run; checksum plus a dated record
#             unregenerable no command produces it; provenance recorded instead
#
# Adding a committed artefact without adding it here fails
# test_every_committed_artefact_has_a_recipe. That is deliberate: an artefact
# with no recipe is the strongest form of drift, and this repository had one.

recipes:
- artefact: data/processed/methane_composite_2018.tif
  command: python scripts/compute_methane_composite.py --checkpoint data/interim/extent_2018.npz
    --export data/processed/methane_composite_2018 --export-csv data/processed/methane_coverage_2018.csv
  label: 2018 methane composite
  cost: 28.9 GB and about two hours to build the checkpoint; seconds to export from it
  seconds: 1
  inputs: network
  compare: bytes
  verified: on_demand
  note: 'The export is instant from a checkpoint. What is expensive is building the checkpoint:
    compute_methane_composite.py --run --max-hours 3, which streams 28.9 GB one granule at
    a time. The checkpoint is gitignored, so this cannot be verified on a fresh clone.'
  sha256: 393e365a8f26c7c142bea08b056a2290482b1401e4d8ff46d5b135a614da9b24
  last_verified: '2026-09-04'
  last_verified_by: the extent re-run, which executed compute_methane_composite.py --run end
    to end over all 578 granules and exported from the result
- artefact: data/processed/urban_disagreement_2018.csv
  command: python scripts/decompose_urban_disagreement.py --write
  label: GAIA-GISA quantity and allocation disagreement
  cost: about ten seconds over two 992 by 1056 rasters
  seconds: 12
  inputs: committed
  compare: bytes
  verified: continuously
  note: >-
    Reads the two committed extent rasters, the province boundaries and the
    analysis grid, all committed, so it regenerates from a fresh clone. Reports
    both the 868 m pixel grid and the 0.25 degree lattice, because misallocation
    within a lattice cell cancels when the cell mean is taken and only the
    surviving part reaches the regression.
- artefact: data/processed/ccdrice_polygons_yrd_2026.csv
  command: python scripts/summarise_ccdrice_polygons.py --download
  label: CCD-Rice validation polygon counts for the four provinces
  cost: a 1.9 MB download and a parquet read, a few seconds
  seconds: 6
  inputs: network
  compare: bytes
  verified: on_demand
  note: >-
    Counts only. The polygons themselves are not committed: the deposit is
    CC-BY and 1.9 MB, so a Tier 2 task can fetch them, and what it needs first
    is the shape of the sample. The script verifies the deposit's published MD5
    before reading and deletes the parquet afterwards. Network tier because the
    input is a figshare deposit rather than a committed file.
  last_verified: '2026-09-13'
  last_verified_by: the dataset inventory verification pass, which ran the recipe
    end to end and compared the result byte for byte
  sha256: 9486490755344e24dd90c61f84760ee146f50d52476771c2d219e22a2d8b2166
- artefact: data/processed/methane_coverage_2018.csv
  command: python scripts/compute_methane_composite.py --checkpoint data/interim/extent_2018.npz
    --export data/processed/methane_composite_2018 --export-csv data/processed/methane_coverage_2018.csv
  label: composite coverage table
  cost: seconds from the checkpoint
  seconds: 1
  inputs: network
  compare: bytes
  verified: on_demand
  note: Written by the same command as the composite raster.
  sha256: 9df96581ebe96bbe0332ec7d3b0fe5c837fce5d40e1654677da29c156376dcc4
  last_verified: '2026-09-04'
  last_verified_by: the extent re-run, which executed compute_methane_composite.py --run end
    to end over all 578 granules and exported from the result
- artefact: data/processed/methane_covariates_2018.tif
  command: python scripts/compute_methane_composite.py --checkpoint data/interim/extent_2018.npz
    --export-covariates data/processed/methane_covariates_2018
  label: covariate companion
  cost: seconds from the checkpoint
  seconds: 1
  inputs: network
  compare: bytes
  verified: on_demand
  note: Same pass as the composite; exporting costs nothing.
  sha256: 0b58e81464bf05bb762e00f5dc6ea808deb0426002b64bd477f91403f31fb462
  last_verified: '2026-09-04'
  last_verified_by: the extent re-run, which executed compute_methane_composite.py --run end
    to end over all 578 granules and exported from the result
- artefact: data/processed/methane_covariates_2018.csv
  command: python scripts/compute_methane_composite.py --checkpoint data/interim/extent_2018.npz
    --export-covariates data/processed/methane_covariates_2018
  label: covariate table
  cost: seconds from the checkpoint
  seconds: 1
  inputs: network
  compare: bytes
  verified: on_demand
  note: null
  sha256: a5e6d20de5785d14d6298ef061dd79c57449440436f73501e5e70d7e1a0d43b6
  last_verified: '2026-09-04'
  last_verified_by: the extent re-run, which executed compute_methane_composite.py --run end
    to end over all 578 granules and exported from the result
- artefact: data/processed/methane_deseasonalised_2018.tif
  command: python scripts/compute_methane_composite.py --checkpoint data/interim/extent_2018.npz
    --export-deseasonalised data/processed/methane_deseasonalised_2018
  label: deseasonalised field
  cost: seconds from the checkpoint
  seconds: 1
  inputs: network
  compare: bytes
  verified: on_demand
  note: null
  sha256: 8e4049d9b5eed4b0eada65a8ac0518958986a33d2b54fdd6d99611527b43372b
  last_verified: '2026-09-04'
  last_verified_by: the extent re-run, which executed compute_methane_composite.py --run end
    to end over all 578 granules and exported from the result
- artefact: data/processed/methane_deseasonalised_2018.csv
  command: python scripts/compute_methane_composite.py --checkpoint data/interim/extent_2018.npz
    --export-deseasonalised data/processed/methane_deseasonalised_2018
  label: deseasonalised table
  cost: seconds from the checkpoint
  seconds: 1
  inputs: network
  compare: bytes
  verified: on_demand
  note: null
  sha256: 05f10f3d4d63a95d63aa149dd6f912acba20bd55ff91a5bbfd2b781e6d092be9
  last_verified: '2026-09-04'
  last_verified_by: the extent re-run, which executed compute_methane_composite.py --run end
    to end over all 578 granules and exported from the result
- artefact: data/processed/analysis_grid_2018.csv
  command: python scripts/build_analysis_grid.py --rice-source nesdc --write
  label: analysis grid
  cost: about 3 minutes over the local rice and urban rasters
  seconds: 180
  inputs: local
  compare: bytes
  verified: on_local
  note: --rice-source nesdc is what the committed file was built from and needs the FTP rasters.
    --rice-source scidb is the anonymous route and reproduces every column except rice_fraction_combined,
    which it lowers in 190 rows because that product folds the double-season class into background.
    Both are legitimate; the committed file is the nesdc one.
- artefact: data/processed/impervious_gisa_2018.csv
  command: python scripts/build_analysis_grid.py --urban-source gisa --impervious-only --write
    --out data/processed/impervious_gisa_2018.csv
  label: GISA impervious layer
  cost: about 3 minutes over the local GISA rasters
  seconds: 178
  inputs: local
  compare: bytes
  verified: on_local
  note: --impervious-only exists because the committed file carries four columns and the script
    previously had no flag that produced them; the reduction was done by hand and never written
    down.
- artefact: data/processed/urban_area_by_province.csv
  command: python scripts/compute_urban_areas.py --write
  label: provincial urban areas
  cost: about 20 seconds over the local GAIA tiles
  seconds: 20
  inputs: local
  compare: subset
  verified: on_local
  note: Reproduces 148 of 160 rows exactly. The other 12 use GADM 4.1 boundaries, which cannot
    be redistributed, so they need a user-supplied copy via --gadm.
- artefact: data/processed/rice_area_by_province.csv
  command: python scripts/compute_rice_areas.py --write
  label: provincial rice areas
  cost: a few seconds over the local GloRice files
  seconds: 4
  inputs: local
  compare: subset
  verified: on_local
  note: Reproduces 28 of 36 rows exactly. The other 8 come from SPAM, for which no fetch module
    exists; they are carried for completeness and used in no analysis.
- artefact: data/processed/baseline_results_2018.csv
  command: python scripts/run_baselines.py --covariates data/processed/methane_covariates_2018.csv
    --write
  label: baselines
  cost: under a second
  seconds: 1
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: data/processed/baseline_results_deseasonalised_2018.csv
  command: python scripts/run_baselines.py --target ch4_deseasonalised_ppb --target-from data/processed/methane_deseasonalised_2018.csv
    --covariates data/processed/methane_covariates_2018.csv --out data/processed/baseline_results_deseasonalised_2018.csv
    --write
  label: baselines, deseasonalised
  cost: under a second
  seconds: 1
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: data/processed/deseasonalisation_2018.csv
  command: python scripts/test_deseasonalisation.py --write
  label: deseasonalisation test
  cost: under a second
  seconds: 1
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: data/processed/inversion_dofs_2018.csv
  command: python scripts/estimate_inversion_dofs.py --write
  label: expected inversion DOFS
  cost: about ten seconds
  seconds: 20
  inputs: local
  compare: bytes
  verified: on_local
  note: Needs data/interim/extent_2018.npz for the per-granule cell bitmaps, which are the
    only source of observation days per cell and are not in any committed artefact. This is
    a reimplementation of the IMI preview's published DOFS formula over this domain, not an
    IMI preview run; the script's docstring says so and so does notes/decisions.md.
- artefact: data/processed/albedo_confounder_2018.csv
  command: python scripts/test_albedo_confounder.py --write
  label: albedo confounder test
  cost: under a second
  seconds: 1
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: data/processed/buffered_loo_2018.csv
  command: python scripts/buffered_loo_curve.py --write
  label: buffered leave-one-out decay curve
  cost: about two seconds
  seconds: 5
  inputs: committed
  compare: bytes
  verified: continuously
  note: Radii chosen from residual_range_2018.csv rather than arbitrarily, which is why that
    recipe gates this one. Read the r2_above_constant column rather than held_out_r2, because
    a constant predictor's skill also falls as the buffer grows -- the training mean drifts
    from the held-out cell's neighbourhood -- and the difference removes that common term.
- artefact: data/processed/residual_range_2018.csv
  command: python scripts/measure_residual_range.py --write
  label: residual autocorrelation ranges
  cost: about fifteen seconds
  seconds: 30
  inputs: committed
  compare: bytes
  verified: continuously
  note: Roberts et al.'s rule wants the residual's autocorrelation range and this repository
    had only the target field's. The variogram reproduces the committed 102 km half-sill for
    the field itself at 103.2 km, which is the check that the method agrees with the earlier
    one. The block is 111 km north-south by 95 km east-west, not one figure.
- artefact: data/processed/correlation_dof_2018.csv
  command: python scripts/correct_correlation_dof.py --write
  label: correlations corrected for spatial dependence
  cost: about twenty seconds
  seconds: 40
  inputs: committed
  compare: bytes
  verified: continuously
  note: Reads albedo_confounder_2018.csv and carries its coefficients forward unchanged,
    correcting only the degrees of freedom. Adds the blended field's correlations, which
    existed only in data/processed/README.md prose, and the raw field's, on which the
    albedo figure's caption rests a survival claim. Twenty-seven of seventy-two
    correlations lose significance under the correction.
- artefact: data/processed/albedo_correction_2018.csv
  command: python scripts/test_albedo_correction.py --write
  label: albedo correction test
  cost: under a second
  seconds: 1
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: data/processed/alternative_predictors_2018.csv
  out_flag: --out-baselines
  command: python scripts/test_alternative_predictors.py --write
  label: predictor robustness
  cost: a second, over four prebuilt grids
  seconds: 1
  inputs: local
  compare: bytes
  verified: on_local
  note: Reads the four grids under data/interim/alt_grids, which are gitignored and built
    by build_analysis_grid.py with each --urban-source and --rice-source pair.
- artefact: data/processed/predictor_comparison_2018.csv
  out_flag: --out-comparison
  command: python scripts/test_alternative_predictors.py --write
  label: predictor comparison
  cost: a second, over four prebuilt grids
  seconds: 1
  inputs: local
  compare: bytes
  verified: on_local
  note: Written by the same command as alternative_predictors_2018.csv.
- artefact: figures/study_area.png
  command: python scripts/make_study_area_figure.py
  label: study area figure
  cost: about a second
  seconds: 1
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: figures/study_area.pdf
  command: python scripts/make_study_area_figure.py
  label: study area figure, vector
  cost: about a second
  seconds: 1
  inputs: committed
  compare: bytes
  verified: continuously
  note: Byte-reproducible because src/figures/output.py drops the PDF CreationDate; left in,
    matplotlib stamps the wall clock and no two runs agree.
- artefact: figures/coverage_saturation_2018.png
  command: python scripts/make_coverage_figure.py
  label: coverage figure
  cost: under a second
  seconds: 1
  inputs: local
  compare: bytes
  verified: on_local
  note: Reads the gitignored 2018 checkpoint, so it cannot be verified on a fresh clone.
- artefact: figures/coverage_saturation_2018.pdf
  command: python scripts/make_coverage_figure.py
  label: coverage figure, vector
  cost: under a second
  seconds: 1
  inputs: local
  compare: bytes
  verified: on_local
  note: null
- artefact: data/processed/urban_area_by_province_gisa.csv
  command: null
  label: GISA provincial urban areas
  cost: null
  seconds: 0
  inputs: none
  compare: none
  verified: unregenerable
  note: 'The one committed artefact no command produces. It was written by hand in
    commit f6b1b0c, which added it and impervious_gisa_2018.csv together and added no
    code for either. Four rows: GISA impervious area per province in 2018 against
    GAIA''s and the thesis''s, on Natural Earth boundaries. compute_urban_areas.py
    cannot produce it, being specific to GAIA''s tile naming and its downward year
    encoding, where GISA counts upward. It is registered here rather than left out so
    that test_every_committed_artefact_has_a_recipe keeps passing honestly: an
    artefact with no recipe is a fact about the repository, and hiding it by omitting
    it from the registry is how this class of problem starts.'
- artefact: figures/methane_composite_2018.png
  command: python scripts/make_composite_figure.py
  label: methane composite figure
  cost: about a second
  seconds: 1
  inputs: committed
  compare: bytes
  verified: continuously
  note: 'Reads only committed artefacts: methane_composite_2018.tif and the reference
    boundary layers. Runs on a fresh clone.'
- artefact: figures/methane_composite_2018.pdf
  command: python scripts/make_composite_figure.py
  label: methane composite figure, vector
  cost: about a second
  seconds: 1
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: data/processed/urban_extent_gaia.tif
  command: python scripts/compute_urban_extent.py --write
  label: urban extent grid, GAIA
  cost: about a minute over 900 MB of gitignored impervious rasters
  seconds: 70
  inputs: local
  compare: bytes
  verified: on_local
  note: Impervious fraction per 1/128 degree cell for 2000, 2010 and 2018, and the exact provincial totals
    behind them. The sixteen rows that overlap the two older tables reproduce them to 1.3e-05, which is
    the check that the two products' opposite year encodings are both applied the right way round. Written
    together by one command; the verifier is handed the path of the artefact it is checking and the script
    writes its siblings beside it.
- artefact: data/processed/urban_extent_gisa.tif
  command: python scripts/compute_urban_extent.py --write
  label: urban extent grid, GISA
  cost: about a minute over 900 MB of gitignored impervious rasters
  seconds: 70
  inputs: local
  compare: bytes
  verified: on_local
  note: Impervious fraction per 1/128 degree cell for 2000, 2010 and 2018, and the exact provincial totals
    behind them. The sixteen rows that overlap the two older tables reproduce them to 1.3e-05, which is
    the check that the two products' opposite year encodings are both applied the right way round. Written
    together by one command; the verifier is handed the path of the artefact it is checking and the script
    writes its siblings beside it.
- artefact: data/processed/urban_extent_totals.csv
  command: python scripts/compute_urban_extent.py --write
  label: urban extent provincial totals
  cost: about a minute over 900 MB of gitignored impervious rasters
  seconds: 70
  inputs: local
  compare: bytes
  verified: on_local
  note: Impervious fraction per 1/128 degree cell for 2000, 2010 and 2018, and the exact provincial totals
    behind them. The sixteen rows that overlap the two older tables reproduce them to 1.3e-05, which is
    the check that the two products' opposite year encodings are both applied the right way round. Written
    together by one command; the verifier is handed the path of the artefact it is checking and the script
    writes its siblings beside it.
- artefact: data/processed/landcover_window_impervious_gisa.tif
  command: python scripts/clip_landcover_window.py --write
  label: native window, GISA
  cost: seconds, from gitignored raw rasters
  seconds: 6
  inputs: local
  compare: bytes
  verified: on_local
  note: 'One 5.7 by 3.7 km window at the product''s own resolution and with its own pixel values, not
    a mask: the figure applies the selector, so the rule that decides what counts is one object. Written
    together by one command; the verifier is handed the path of the artefact it is checking and the script
    writes its siblings beside it.'
- artefact: data/processed/landcover_window_impervious_gaia.tif
  command: python scripts/clip_landcover_window.py --write
  label: native window, GAIA
  cost: seconds, from gitignored raw rasters
  seconds: 6
  inputs: local
  compare: bytes
  verified: on_local
  note: 'One 5.7 by 3.7 km window at the product''s own resolution and with its own pixel values, not
    a mask: the figure applies the selector, so the rule that decides what counts is one object. Written
    together by one command; the verifier is handed the path of the artefact it is checking and the script
    writes its siblings beside it.'
- artefact: data/processed/landcover_window_rice_nesdc.tif
  command: python scripts/clip_landcover_window.py --write
  label: native window, NESDC rice
  cost: seconds, from gitignored raw rasters
  seconds: 6
  inputs: local
  compare: bytes
  verified: on_local
  note: 'One 5.7 by 3.7 km window at the product''s own resolution and with its own pixel values, not
    a mask: the figure applies the selector, so the rule that decides what counts is one object. Written
    together by one command; the verifier is handed the path of the artefact it is checking and the script
    writes its siblings beside it.'
- artefact: figures/landcover_native.png
  command: python scripts/make_landcover_figure.py
  label: native land cover figure
  cost: about a second
  seconds: 2
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: figures/landcover_native.pdf
  command: python scripts/make_landcover_figure.py
  label: native land cover figure, vector
  cost: about a second
  seconds: 2
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: figures/urban_change.png
  command: python scripts/make_urban_change_figure.py
  label: urban change figure
  cost: about a second
  seconds: 2
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: figures/urban_change.pdf
  command: python scripts/make_urban_change_figure.py
  label: urban change figure, vector
  cost: about a second
  seconds: 2
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: data/processed/rice_extent_2018.tif
  command: python scripts/compute_rice_extent.py --write
  label: rice extent grid
  cost: about three minutes over 3.5 GB of gitignored rice rasters
  seconds: 155
  inputs: local
  compare: bytes
  verified: on_local
  note: 'Single-season, double-season and classified-share per 1/128 degree cell for 2018. Each of the
    four rasters is masked by the province it is named for, never by the union: the files overlap and
    a union mask assesses shared ground once per file. The script asserts the assessed area against the
    province polygons before writing and reproduces Anhui''s recorded 86.1 percent footprint independently.
    Written with the totals table by one command; the verifier is handed the path of the artefact it is
    checking.'
- artefact: data/processed/rice_extent_totals_2018.csv
  command: python scripts/compute_rice_extent.py --write
  label: rice extent provincial totals
  cost: about three minutes over 3.5 GB of gitignored rice rasters
  seconds: 155
  inputs: local
  compare: bytes
  verified: on_local
  note: Rice area by province and season with both denominators beside it, the assessed area and the province
    polygon. Its Anhui total of 22,594.6 km2 reproduces the 22,594.7 recorded in notes/decisions.md from
    a separate route. Written by the same command as the grid.
- artefact: figures/landcover_regional.png
  command: python scripts/make_landcover_regional_figure.py
  label: regional land cover figure
  cost: about a second
  seconds: 2
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: figures/landcover_regional.pdf
  command: python scripts/make_landcover_regional_figure.py
  label: regional land cover figure, vector
  cost: about a second
  seconds: 2
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: data/processed/baseline_predictions_2018.csv
  command: python scripts/compute_baseline_predictions.py --write
  label: held-out baseline predictions
  cost: about ten seconds over the committed grid and covariates
  seconds: 12
  inputs: committed
  compare: bytes
  verified: continuously
  note: One row per cell per model for the four models the diagnostic figures draw, every value out of
    fold. The script refuses to write unless the metrics recomputed from the pooled predictions reproduce
    baseline_results_2018.csv, which is what says the figures and the table describe one fit.
- artefact: figures/buffered_decay.png
  command: python scripts/make_buffered_decay_figure.py
  label: buffered decay curve figure
  cost: about two seconds
  seconds: 3
  inputs: committed
  compare: bytes
  verified: continuously
  note: >-
    Reads data/processed/buffered_loo_2018.csv and the spatial null's
    leave-one-province-out row from data/processed/baseline_results_2018.csv.
    Both are committed, so this regenerates from a fresh clone. Nothing in the
    figure module recomputes a fold; tests/test_figures_capability_curves.py
    asserts every drawn line against its artefact column elementwise.
- artefact: figures/buffered_decay.pdf
  command: python scripts/make_buffered_decay_figure.py
  label: buffered decay curve figure, vector
  cost: about two seconds
  seconds: 3
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: figures/capability.png
  command: python scripts/make_capability_figure.py
  label: capability assessment figure
  cost: about two seconds
  seconds: 3
  inputs: committed
  compare: bytes
  verified: continuously
  note: >-
    Reads data/processed/inversion_dofs_2018.csv alone. The sensitivity
    expression is not evaluated here; the sweep, the bisected threshold
    crossings, the per-cell sensitivity quantiles and the two prior-free
    emission thresholds are all read from that file.
- artefact: figures/capability.pdf
  command: python scripts/make_capability_figure.py
  label: capability assessment figure, vector
  cost: about two seconds
  seconds: 3
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: figures/observed_predicted.png
  command: python scripts/make_observed_predicted_figure.py
  label: observed against predicted figure
  cost: about two seconds
  seconds: 3
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: figures/observed_predicted.pdf
  command: python scripts/make_observed_predicted_figure.py
  label: observed against predicted figure, vector
  cost: about two seconds
  seconds: 3
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: figures/residual_field.png
  command: python scripts/make_residual_field_figure.py
  label: model field and residual figure
  cost: about eight seconds
  seconds: 12
  inputs: committed
  compare: bytes
  verified: continuously
  note: The permutation test behind the residual's Moran's I runs on a fixed seed, so the number the
    figure draws is the same on every run.
- artefact: figures/residual_field.pdf
  command: python scripts/make_residual_field_figure.py
  label: model field and residual figure, vector
  cost: about eight seconds
  seconds: 12
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: figures/framework_pipeline.png
  command: python scripts/make_framework_pipeline_figure.py
  label: pipeline framework figure
  cost: about two seconds
  seconds: 4
  inputs: committed
  compare: bytes
  verified: continuously
  note: The figure quotes the tiering of this register and the number of figures in it, so registering
    a new figure changes this one and the recipe reproduces the change.
- artefact: figures/framework_pipeline.pdf
  command: python scripts/make_framework_pipeline_figure.py
  label: pipeline framework figure, vector
  cost: about two seconds
  seconds: 4
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: figures/framework_reproduction.png
  command: python scripts/make_framework_reproduction_figure.py
  label: reproduction status framework figure
  cost: about three seconds
  seconds: 5
  inputs: committed
  compare: bytes
  verified: continuously
  note: The script refuses to write unless every ERRATA.md section a cell cites exists and every phrase
    the cell declares as evidence appears in those sections.
- artefact: figures/framework_reproduction.pdf
  command: python scripts/make_framework_reproduction_figure.py
  label: reproduction status framework figure, vector
  cost: about three seconds
  seconds: 5
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: figures/albedo_collinearity.png
  command: python scripts/make_albedo_collinearity_figure.py
  label: albedo collinearity figure
  cost: about four seconds
  seconds: 8
  inputs: committed
  compare: bytes
  verified: continuously
  note: Every correlation is recomputed from analysis_grid_2018.csv and methane_covariates_2018.csv at
    build time, and the figure refuses to build unless the bias-corrected values reproduce albedo_confounder_2018.csv.
- artefact: figures/albedo_collinearity.pdf
  command: python scripts/make_albedo_collinearity_figure.py
  label: albedo collinearity figure, vector
  cost: about four seconds
  seconds: 8
  inputs: committed
  compare: bytes
  verified: continuously
  note: null
- artefact: data/processed/methane_blended_2018.tif
  command: python scripts/compute_blended_composite.py --write
  label: blended TROPOMI+GOSAT composite
  cost: about seven minutes and 336 MB of transfer
  seconds: 430
  inputs: network
  compare: bytes
  verified: on_demand
  sha256: 3ebc85c1643d653259aa0245152bafbf18d78e8a2efbe7d58a2eac26c157e138
  last_verified: '2026-09-10'
  last_verified_by: full run over the 223 productive orbits, 335.6 MB in 1,385 range requests
  note: Reads four variables from each of the 223 orbits that carry an in-box sounding, over HTTP range
    requests, so the whole 23 GiB record is never fetched. Cheaper than the operational composite by two
    orders of magnitude and still on_demand because it needs the network.
- artefact: data/processed/methane_blended_2018.csv
  command: python scripts/compute_blended_composite.py --write
  label: blended composite, per-cell table
  cost: about seven minutes and 336 MB of transfer
  seconds: 430
  inputs: network
  compare: bytes
  verified: on_demand
  sha256: f86bd47bcc38ac287967726bba42e51613a0273449fda32b48a1e0c80e753a5a
  last_verified: '2026-09-10'
  last_verified_by: full run over the 223 productive orbits, 335.6 MB in 1,385 range requests
  note: null
- artefact: data/processed/baseline_results_blended_2018.csv
  command: python scripts/run_baselines.py --target ch4_blended_ppb --target-from data/processed/methane_blended_2018.csv
    --covariates data/processed/methane_covariates_2018.csv --out data/processed/baseline_results_blended_2018.csv
    --write
  label: baselines on the blended field
  cost: about a minute
  seconds: 70
  inputs: committed
  compare: bytes
  verified: continuously
  note: The three covariates absent from the blended granules are present here, because methane_covariates_2018.csv
    was averaged over the identical soundings, so the full suite runs unchanged.
